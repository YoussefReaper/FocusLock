<?xml version="1.0" encoding="utf-8"?>
<!-- Structure only; colour and type come from UiPrefs.Tokens at runtime. -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/folderRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="28dp">

    <TextView
        android:id="@+id/tvFolderTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Choose your video folder"
        android:gravity="center"
        android:layout_marginBottom="14dp" />

    <TextView
        android:id="@+id/tvFolderSubtitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:lineSpacingExtra="4dp"
        android:layout_marginBottom="36dp" />

    <TextView
        android:id="@+id/btnPickFolder"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="Choose a folder"
        android:layout_marginBottom="16dp" />

    <TextView
        android:id="@+id/tvChosenPath"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_marginBottom="28dp" />

    <TextView
        android:id="@+id/btnConfirm"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="Use this folder" />

    <TextView
        android:id="@+id/tvFolderWarning"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_marginTop="18dp" />

</LinearLayout>
